import { ReplaceWithspacePipe } from './replace-withspace.pipe';

describe('ReplaceWithspacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWithspacePipe();
    expect(pipe).toBeTruthy();
  });
});
